global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: corpus-platform
    metrics_path: /metrics
    bearer_token_file: /etc/prometheus/secrets/metrics_bearer_token
    static_configs:
      - targets: [web:8000]
